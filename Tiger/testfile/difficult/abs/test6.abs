LetExp(
 DecList(
  VarDec(a,
   NilExp(),
   true),
  DecList(
   TypeDec(a,
    RecordTy(
     Fieldlist())),
   DecList(
    VarDec(a,
     a,
     NilExp(),
     true),
    DecList(
     FunctionDec(g
      Fieldlist(),
      a
      NilExp(),
      FunctionDec(h
       Fieldlist(),
       a
       SeqExp(
        ExpList(
         NilExp())),
       FunctionDec(i
        Fieldlist(),
        a
        SeqExp(
         ExpList(
          NilExp(),
          ExpList(
           NilExp()))),
        FunctionDec(w
         Fieldlist(
          a
          a,
          true,
          Fieldlist()),
         CallExp(w,
          ExpList(
           NilExp())),
         FunctionDec())))),
     DecList())))),
 SeqExp(
  ExpList(
   AssignExp(
    SimpleVar(a),
    NilExp()),
   ExpList(
    IfExp(
     OpExp(
      NE,
      varExp(
       SimpleVar(a)),
      NilExp()),
     SeqExp(
      ExpList())),
    ExpList(
     IfExp(
      OpExp(
       NE,
       NilExp(),
       varExp(
        SimpleVar(a))),
      SeqExp(
       ExpList())),
     ExpList(
      IfExp(
       OpExp(
        EQ,
        varExp(
         SimpleVar(a)),
        NilExp()),
       SeqExp(
        ExpList())),
      ExpList(
       IfExp(
        OpExp(
         EQ,
         NilExp(),
         NilExp()),
        SeqExp(
         ExpList())))))))))