SeqExp(
 ExpList(
  CallExp(print,
   ExpList(
    StringExp(sdfklj))),
  ExpList(
   CallExp(flush,
    ExpList()),
   ExpList(
    CallExp(getchar,
     ExpList()),
    ExpList(
     CallExp(ord,
      ExpList(
       StringExp(sdfklj))),
     ExpList(
      CallExp(chr,
       ExpList(
        IntExp(1))),
      ExpList(
       CallExp(size,
        ExpList(
         StringExp(sdlkjf))),
       ExpList(
        CallExp(substring,
         ExpList(
          StringExp(sdlfkj),
          ExpList(
           IntExp(1),
           ExpList(
            IntExp(2))))),
        ExpList(
         CallExp(concat,
          ExpList(
           StringExp(sdlfkj),
           ExpList(
            StringExp(sdfklj)))),
         ExpList(
          CallExp(not,
           ExpList(
            IntExp(1))),
          ExpList(
           CallExp(exit,
            ExpList(
             IntExp(0))))))))))))))