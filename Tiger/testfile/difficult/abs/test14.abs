LetExp(
 DecList(
  VarDec(a,
   IntExp(1),
   true),
  DecList(
   TypeDec(a,
    RecordTy(
     Fieldlist(
      a
      int,
      true,
      Fieldlist(
       b
       int,
       true,
       Fieldlist())))),
   DecList(
    VarDec(b,
     a,
     NilExp(),
     true),
    DecList(
     VarDec(b,
      a,
      RecordExp(a,
       FieldExpList(
a,
        IntExp(1),
        FieldExpList(
b,
         IntExp(1),
         FieldExpList()))),
      true),
     DecList(
      FunctionDec(g
       Fieldlist(
        a
        int,
        true,
        Fieldlist(
         b
         int,
         true,
         Fieldlist(
          a
          int,
          true,
          Fieldlist()))),
       int
       varExp(
        SimpleVar(a)),
       FunctionDec()),
      DecList(
       TypeDec(x,
        ArrayTy(int)),
       DecList(
        VarDec(c,
         x,
         ArrayExp(x,
          IntExp(10),
          IntExp(0)),
         true),
        DecList()))))))),
 SeqExp(
  ExpList(
   IntExp(1),
   ExpList(
    StringExp(sdlkfj),
    ExpList(
     NilExp(),
     ExpList(
      varExp(
       SimpleVar(a)),
      ExpList(
       AssignExp(
        SimpleVar(a),
        varExp(
         SimpleVar(a))),
       ExpList(
        AssignExp(
         SimpleVar(b),
         RecordExp(a,
          FieldExpList(
a,
           IntExp(1),
           FieldExpList(
b,
            IntExp(1),
            FieldExpList())))),
        ExpList(
         SeqExp(
          ExpList(
           IntExp(1),
           ExpList(
            IntExp(2),
            ExpList(
             IntExp(3),
             ExpList(
              IntExp(4),
              ExpList(
               IntExp(5))))))),
         ExpList(
          CallExp(g,
           ExpList(
            IntExp(1),
            ExpList(
             IntExp(2),
             ExpList(
              IntExp(3))))),
          ExpList(
           CallExp(g,
            ExpList(
             IntExp(1),
             ExpList(
              IntExp(2)))),
           ExpList(
            CallExp(g,
             ExpList()),
            ExpList(
             CallExp(g,
              ExpList(
               IntExp(1),
               ExpList(
                IntExp(2),
                ExpList(
                 IntExp(3),
                 ExpList(
                  IntExp(4)))))),
             ExpList(
              CallExp(g,
               ExpList(
                StringExp(sdlkfj),
                ExpList(
                 IntExp(1)))),
              ExpList(
               AssignExp(
                SimpleVar(c),
                ArrayExp(x,
                 IntExp(10),
                 IntExp(1))),
               ExpList(
                IfExp(
                 SeqExp(
                  ExpList(
                   IntExp(1))),
                 SeqExp(
                  ExpList())),
                ExpList(
                 IfExp(
                  IntExp(1),
                  IntExp(1),
                  IntExp(1)),
                 ExpList(
                  WhileExp(
                   IntExp(1),
                   SeqExp(
                    ExpList()))
,
                  ExpList(
                   ForExp(
                    VarDec(i,
                     IntExp(1),
                     true),
                    IntExp(2),
                    BreakExp()))))))))))))))))))))